services:
  lemonde-full-rss:
    build: .
    restart: unless-stopped
    env_file: .env
    volumes:
      - ./data:/app/data
      - ./config:/app/config:ro
      - ./secrets:/app/secrets:ro
    expose: ["8000"]
    networks: [internal, proxy]
networks:
  internal:
    driver: bridge
